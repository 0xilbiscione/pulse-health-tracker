DATABASE_URL="file:./dev.db"

# Generate with: openssl rand -hex 32
AUTH_SECRET="replace-with-a-random-32-byte-hex-string"

# Public canonical URL — lets Auth.js issue correct secure cookies behind the
# HTTPS tunnel. Omit for plain local dev on http://localhost:3000.
AUTH_URL="https://fit.metricbase.org"
